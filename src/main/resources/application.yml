spring:
  profiles:
    default: local
  datasource:
    url: jdbc:mysql://localhost:3306/learn_redis
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    org.springframework.cache: trace

---
# prod 환경
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://my-rds.crimak2o6r6m.ap-northeast-2.rds.amazonaws.com:3306/mydb
    username: admin
    password: password